<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
<title>Madeleine Spa NJ — Client Intake Form</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Jost:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --gold: #B8860B;
    --gold-light: #D4A843;
    --gold-pale: #F5EDD8;
    --cream: #FAF6EF;
    --brown-dark: #3C2A1A;
    --brown-mid: #6B4C2A;
    --brown-light: #A07850;
    --text: #2C1F0F;
    --text-muted: #7A6048;
    --border: #D4B896;
    --border-light: #EAD9C0;
    --white: #FFFDF9;
    --danger: #8B2020;
  }

  body {
    background: var(--cream);
    font-family: 'Jost', sans-serif;
    color: var(--text);
    font-size: 16px;
    font-weight: 300;
    line-height: 1.6;
    min-height: 100vh;
  }

  .page-wrap {
    max-width: 820px;
    margin: 0 auto;
    padding: 40px 24px 80px;
  }

  /* HEADER */
  .header {
    text-align: center;
    padding: 48px 24px 40px;
    border-bottom: 1px solid var(--border);
    margin-bottom: 48px;
    position: relative;
  }
  .header::before {
    content: '';
    position: absolute;
    top: 0; left: 50%;
    transform: translateX(-50%);
    width: 60px; height: 2px;
    background: var(--gold);
  }
  .spa-name {
    font-family: 'Cormorant Garamond', serif;
    font-size: 42px;
    font-weight: 300;
    letter-spacing: 0.12em;
    color: var(--brown-dark);
    line-height: 1.1;
  }
  .spa-name span { color: var(--gold); }
  .spa-tagline {
    font-size: 12px;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--text-muted);
    margin-top: 10px;
  }
  .form-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: 20px;
    font-weight: 400;
    color: var(--brown-mid);
    margin-top: 20px;
    font-style: italic;
  }

  /* SECTIONS */
  .section {
    background: var(--white);
    border: 1px solid var(--border-light);
    border-radius: 4px;
    padding: 36px 40px;
    margin-bottom: 28px;
  }
  .section-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: 22px;
    font-weight: 500;
    color: var(--brown-dark);
    margin-bottom: 6px;
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .section-title::after {
    content: '';
    flex: 1;
    height: 1px;
    background: var(--border-light);
  }
  .section-sub {
    font-size: 13px;
    color: var(--text-muted);
    letter-spacing: 0.05em;
    margin-bottom: 28px;
  }

  /* FORM FIELDS */
  .field-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    margin-bottom: 20px;
  }
  .field-row.triple {
    grid-template-columns: 2fr 1fr 1fr;
  }
  .field-row.full {
    grid-template-columns: 1fr;
  }
  .field {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  label {
    font-size: 11px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--text-muted);
    font-weight: 400;
  }
  input[type="text"],
  input[type="email"],
  input[type="tel"],
  input[type="date"],
  textarea {
    background: var(--cream);
    border: 1px solid var(--border);
    border-radius: 2px;
    padding: 12px 16px;
    font-family: 'Jost', sans-serif;
    font-size: 15px;
    font-weight: 300;
    color: var(--text);
    outline: none;
    transition: border-color 0.2s;
    width: 100%;
  }
  input:focus, textarea:focus {
    border-color: var(--gold);
  }
  textarea {
    resize: vertical;
    min-height: 80px;
  }

  /* CHECKBOXES */
  .checkbox-intro {
    font-size: 14px;
    color: var(--text);
    margin-bottom: 20px;
    padding: 16px 20px;
    background: var(--gold-pale);
    border-left: 3px solid var(--gold);
    border-radius: 2px;
    line-height: 1.5;
  }
  .check-columns {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px 32px;
  }
  .check-item {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    padding: 8px 0;
    border-bottom: 1px solid var(--border-light);
    cursor: pointer;
  }
  .check-item:last-child { border-bottom: none; }
  .check-item input[type="checkbox"] {
    appearance: none;
    -webkit-appearance: none;
    width: 20px;
    height: 20px;
    min-width: 20px;
    border: 1.5px solid var(--border);
    border-radius: 2px;
    background: var(--cream);
    cursor: pointer;
    margin-top: 1px;
    position: relative;
    transition: all 0.15s;
  }
  .check-item input[type="checkbox"]:checked {
    background: var(--gold);
    border-color: var(--gold);
  }
  .check-item input[type="checkbox"]:checked::after {
    content: '✓';
    position: absolute;
    top: -1px; left: 3px;
    color: white;
    font-size: 13px;
  }
  .check-label {
    font-size: 14px;
    font-weight: 300;
    color: var(--text);
    line-height: 1.4;
    cursor: pointer;
    padding-top: 1px;
  }

  /* WAIVER */
  .waiver-box {
    background: var(--cream);
    border: 1px solid var(--border-light);
    border-radius: 2px;
    padding: 24px 28px;
    font-size: 14px;
    line-height: 1.8;
    color: var(--text);
    max-height: 280px;
    overflow-y: auto;
    margin-bottom: 20px;
  }
  .waiver-box p { margin-bottom: 12px; }
  .waiver-box p:last-child { margin-bottom: 0; }
  .waiver-box strong {
    font-weight: 500;
    color: var(--brown-dark);
  }
  .waiver-scroll-note {
    font-size: 12px;
    color: var(--text-muted);
    letter-spacing: 0.05em;
    text-align: center;
    margin-bottom: 20px;
    font-style: italic;
  }

  .agree-row {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    padding: 16px 20px;
    background: var(--gold-pale);
    border: 1px solid var(--border);
    border-radius: 2px;
    cursor: pointer;
    margin-bottom: 24px;
  }
  .agree-row input[type="checkbox"] {
    appearance: none;
    -webkit-appearance: none;
    width: 22px;
    height: 22px;
    min-width: 22px;
    border: 1.5px solid var(--gold);
    border-radius: 2px;
    background: white;
    cursor: pointer;
    position: relative;
    transition: all 0.15s;
  }
  .agree-row input[type="checkbox"]:checked {
    background: var(--gold);
    border-color: var(--gold);
  }
  .agree-row input[type="checkbox"]:checked::after {
    content: '✓';
    position: absolute;
    top: 0px; left: 3px;
    color: white;
    font-size: 14px;
  }
  .agree-label {
    font-size: 14px;
    line-height: 1.5;
    cursor: pointer;
  }

  /* SIGNATURE PAD */
  .sig-pad-wrap {
    background: var(--cream);
    border: 1.5px solid var(--border);
    border-radius: 2px;
    position: relative;
    overflow: hidden;
  }
  .sig-canvas {
    display: block;
    width: 100%;
    height: 200px;
    touch-action: none;
    cursor: crosshair;
  }
  .sig-placeholder {
    position: absolute;
    bottom: 16px; left: 0; right: 0;
    text-align: center;
    font-family: 'Cormorant Garamond', serif;
    font-size: 13px;
    color: var(--border);
    letter-spacing: 0.1em;
    pointer-events: none;
    transition: opacity 0.3s;
  }
  .sig-x {
    position: absolute;
    left: 24px; bottom: 14px;
    font-size: 18px;
    color: var(--border);
    pointer-events: none;
  }
  .sig-line {
    position: absolute;
    left: 40px; right: 24px; bottom: 36px;
    height: 1px;
    background: var(--border);
    pointer-events: none;
  }
  .sig-actions {
    display: flex;
    gap: 12px;
    margin-top: 12px;
    justify-content: flex-end;
  }
  .btn-clear {
    background: none;
    border: 1px solid var(--border);
    border-radius: 2px;
    padding: 8px 20px;
    font-family: 'Jost', sans-serif;
    font-size: 13px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--text-muted);
    cursor: pointer;
    transition: all 0.15s;
  }
  .btn-clear:hover { border-color: var(--gold); color: var(--gold); }

  .date-row {
    display: flex;
    align-items: center;
    gap: 16px;
    margin-top: 20px;
  }
  .date-row label {
    white-space: nowrap;
  }

  /* SUBMIT */
  .submit-section {
    text-align: center;
    padding: 40px 0 20px;
  }
  .btn-submit {
    background: var(--brown-dark);
    color: var(--gold-pale);
    border: none;
    padding: 18px 64px;
    font-family: 'Jost', sans-serif;
    font-size: 13px;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    border-radius: 2px;
    cursor: pointer;
    transition: all 0.2s;
    font-weight: 400;
  }
  .btn-submit:hover {
    background: var(--brown-mid);
  }
  .submit-note {
    font-size: 12px;
    color: var(--text-muted);
    margin-top: 14px;
    font-style: italic;
  }

  /* SUCCESS */
  .success-screen {
    display: none;
    text-align: center;
    padding: 80px 40px;
  }
  .success-icon {
    font-size: 48px;
    color: var(--gold);
    margin-bottom: 24px;
  }
  .success-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: 32px;
    font-weight: 300;
    color: var(--brown-dark);
    margin-bottom: 12px;
  }
  .success-msg {
    font-size: 15px;
    color: var(--text-muted);
    max-width: 420px;
    margin: 0 auto 32px;
  }
  .btn-print {
    background: none;
    border: 1.5px solid var(--gold);
    color: var(--gold);
    padding: 14px 40px;
    font-family: 'Jost', sans-serif;
    font-size: 13px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    border-radius: 2px;
    cursor: pointer;
    margin-right: 12px;
  }
  .btn-new {
    background: var(--brown-dark);
    color: var(--gold-pale);
    border: none;
    padding: 14px 40px;
    font-family: 'Jost', sans-serif;
    font-size: 13px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    border-radius: 2px;
    cursor: pointer;
  }

  /* ERROR */
  .error-msg {
    color: var(--danger);
    font-size: 13px;
    margin-top: 6px;
    display: none;
  }
  .field-error input,
  .field-error textarea {
    border-color: var(--danger);
  }
  .required-star { color: var(--gold); }

  /* DIVIDER */
  .ornament {
    text-align: center;
    color: var(--border);
    letter-spacing: 0.4em;
    font-size: 12px;
    margin: 8px 0 24px;
  }

  @media (max-width: 600px) {
    .page-wrap { padding: 20px 16px 60px; }
    .section { padding: 24px 20px; }
    .field-row, .field-row.triple { grid-template-columns: 1fr; }
    .check-columns { grid-template-columns: 1fr; }
    .spa-name { font-size: 32px; }
    .sig-canvas { height: 160px; }
  }

  @media print {
    body { background: white; }
    .btn-submit, .btn-clear, .sig-actions { display: none; }
    .section { box-shadow: none; break-inside: avoid; }
  }
</style>
</head>
<body>

<div class="page-wrap" id="main-form">

  <!-- HEADER -->
  <div class="header">
    <div class="spa-name">Madeleine <span>Spa</span></div>
    <div class="spa-tagline">Cliffside Park, New Jersey &nbsp;·&nbsp; Est. 2008</div>
    <div class="form-title">Client Intake & Consent Form</div>
  </div>

  <!-- PERSONAL INFO -->
  <div class="section">
    <div class="section-title">Personal Information</div>
    <div class="section-sub">Please complete all fields below</div>

    <div class="field-row">
      <div class="field" id="f-first">
        <label>First Name <span class="required-star">*</span></label>
        <input type="text" id="firstName" placeholder="First name" autocomplete="given-name">
        <span class="error-msg">Required</span>
      </div>
      <div class="field" id="f-last">
        <label>Last Name <span class="required-star">*</span></label>
        <input type="text" id="lastName" placeholder="Last name" autocomplete="family-name">
        <span class="error-msg">Required</span>
      </div>
    </div>

    <div class="field-row full">
      <div class="field" id="f-address">
        <label>Address <span class="required-star">*</span></label>
        <input type="text" id="address" placeholder="Street address" autocomplete="street-address">
        <span class="error-msg">Required</span>
      </div>
    </div>

    <div class="field-row triple">
      <div class="field" id="f-city">
        <label>City <span class="required-star">*</span></label>
        <input type="text" id="city" placeholder="City" autocomplete="address-level2">
        <span class="error-msg">Required</span>
      </div>
      <div class="field">
        <label>State</label>
        <input type="text" id="state" placeholder="NJ" autocomplete="address-level1">
      </div>
      <div class="field">
        <label>ZIP Code</label>
        <input type="text" id="zip" placeholder="07010" autocomplete="postal-code">
      </div>
    </div>

    <div class="field-row">
      <div class="field" id="f-phone">
        <label>Telephone <span class="required-star">*</span></label>
        <input type="tel" id="phone" placeholder="(201) 000-0000" autocomplete="tel">
        <span class="error-msg">Required</span>
      </div>
      <div class="field">
        <label>Email Address</label>
        <input type="email" id="email" placeholder="your@email.com" autocomplete="email">
      </div>
    </div>
  </div>

  <!-- MEDICAL HISTORY -->
  <div class="section">
    <div class="section-title">Medical History</div>
    <div class="section-sub">Please indicate any past or current medical conditions</div>

    <div class="checkbox-intro">
      Please place a check next to any condition that currently applies or has applied in the past. This information is strictly confidential and is used solely to ensure your safety during treatment.
    </div>

    <div class="check-columns">

      <div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="lupus">
          <span class="check-label">Lupus or other auto-immune deficiency</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="pregnant">
          <span class="check-label">Currently pregnant</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="bleeding">
          <span class="check-label">Bleeding abnormalities</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="accutane">
          <span class="check-label">Treatment with Accutane in the last year</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="keloid">
          <span class="check-label">Keloid or very thick scarring</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="embolism">
          <span class="check-label">Pulmonary embolism / blood clot</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="bloodthin">
          <span class="check-label">Blood thinning medication</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="acne">
          <span class="check-label">Cystic acne</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="diabetes">
          <span class="check-label">Diabetes</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="darkspots">
          <span class="check-label">Dark spots after pregnancy or skin injury</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="hepatitis">
          <span class="check-label">Hepatitis or any liver condition</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="hirsutism">
          <span class="check-label">Hirsutism (HR)</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="chempeels">
          <span class="check-label">Chemical peels or dermabrasion</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="laserlift">
          <span class="check-label">Laser resurfacing or face lift</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="ovarian">
          <span class="check-label">Polycystic ovarian disease</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="period">
          <span class="check-label">Currently on period</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="varicose">
          <span class="check-label">Varicose vein</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="circulatory">
          <span class="check-label">Circulatory problems</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="pacemaker">
          <span class="check-label">Pacemaker</span>
        </div>
      </div>

      <div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="rheumatoid">
          <span class="check-label">Rheumatoid arthritis "gold" therapy</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="epilepsy">
          <span class="check-label">Light sensitive epilepsy</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="tetracycline">
          <span class="check-label">Treatment with tetracycline</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="psoriasis">
          <span class="check-label">Psoriasis or vitiligo</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="legulcer">
          <span class="check-label">Leg ulcer or phlebitis</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="coumadin">
          <span class="check-label">Coumadin / anti-clotting agents</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="herpes">
          <span class="check-label">Herpes simplex or fever blisters</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="scars">
          <span class="check-label">Scars that turn white or brown</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="hiv">
          <span class="check-label">HIV</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="waxing">
          <span class="check-label">Waxing / plinking / electrolysis</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="transplant">
          <span class="check-label">Transplant anti-rejection drugs</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="tattoo">
          <span class="check-label">Tattoo / permanent make-up</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="implants">
          <span class="check-label">Any implants</span>
        </div>
        <div class="check-item" onclick="toggleCheck(this)">
          <input type="checkbox" name="med" value="highpressure">
          <span class="check-label">High blood pressure</span>
        </div>
      </div>

    </div>

    <div style="margin-top: 24px;">
      <div class="field-row">
        <div class="field">
          <label>Collagen injection or any filler — Location</label>
          <input type="text" id="fillerLocation" placeholder="e.g. lips, cheeks">
        </div>
        <div class="field">
          <label>Date of application</label>
          <input type="date" id="fillerDate">
        </div>
      </div>
    </div>

    <div style="margin-top: 16px;">
      <div class="field full">
        <label>Please list any medications or herbal supplements you are currently taking</label>
        <textarea id="medications" placeholder="List all medications and supplements here..."></textarea>
      </div>
    </div>

  </div>

  <!-- LIABILITY WAIVER -->
  <div class="section">
    <div class="section-title">Liability Waiver & Consent</div>
    <div class="section-sub">Please read the following carefully before signing</div>

    <div class="waiver-box">
      <p>I, <strong>(hereafter referred to as THE CUSTOMER)</strong>, in regards to <strong>Madeleine Spa NJ</strong> (hereafter referred to as THE COMPANY), and the treatments, machines, and products that I am going to use or being used on my body and on my face, agree to the following:</p>

      <p>I release and hereby waive any right to claim any liability against <strong>THE COMPANY</strong> from all legal issues in regards to the services given, equipment, products and/or materials used on my body and/or face.</p>

      <p>I acknowledge that I have no medical conditions that may have adverse reactions. In regards to the use of cosmetic products or cosmetic machinery, I assume full responsibility for any reaction caused by the machine or application of products given by THE COMPANY.</p>

      <p><strong>THE CUSTOMER</strong> understands that cosmetic products may contain ingredients which may cause an allergic reaction in certain people. THE COMPANY is not responsible for any claims due to an allergic reaction.</p>

      <p><strong>THE CUSTOMER</strong> knows that by signing this document, they are releasing THE COMPANY of any claim or complaint relating to the formulation, manufacturer and product application, treatment applied, machine used, or any liability.</p>

      <p><strong>THE CUSTOMER</strong> confirms that he/she is 18 years of age or older and has read and understands the terms above.</p>

      <p><strong>THE CUSTOMER</strong> has read and accepts the above mentioned terms, and completely understands the terms and conditions, and signs this consent/release freely and without any coercion.</p>

      <p>The Customer accepts that his or her pictures and videos that were taken during demos, treatments, or shows may be published on the Company Website, Newsletters, the Company Facebook Page, the Company Instagram, and printed billboard material (Posters, Postcards, etc.).</p>
    </div>
    <div class="waiver-scroll-note">↑ Scroll to read full agreement ↑</div>

    <label class="agree-row" for="agreeCheck">
      <input type="checkbox" id="agreeCheck">
      <span class="agree-label">I have read, fully understand, and agree to the terms of this Liability Waiver & Consent. I confirm that all information I have provided is accurate and complete to the best of my knowledge.</span>
    </label>

  </div>

  <!-- SIGNATURE -->
  <div class="section">
    <div class="section-title">Signature</div>
    <div class="section-sub">Please sign below using your finger or stylus</div>

    <div class="sig-pad-wrap" id="sigPadWrap">
      <canvas class="sig-canvas" id="sigCanvas"></canvas>
      <div class="sig-x">✕</div>
      <div class="sig-line"></div>
      <div class="sig-placeholder" id="sigPlaceholder">Sign here</div>
    </div>
    <div class="sig-actions">
      <button class="btn-clear" onclick="clearSignature()">Clear Signature</button>
    </div>
    <span class="error-msg" id="sig-error" style="display:none; margin-top:4px;">Signature is required</span>

    <div class="date-row">
      <div class="field" style="flex:1;" id="f-sigdate">
        <label>Date <span class="required-star">*</span></label>
        <input type="date" id="sigDate">
        <span class="error-msg">Required</span>
      </div>
    </div>

  </div>

  <!-- NOTES -->
  <div class="section">
    <div class="section-title">Additional Notes</div>
    <div class="section-sub">Any other information you'd like us to know — skin concerns, allergies, treatment goals, special requests, etc.</div>
    <div class="field full">
      <label>Notes</label>
      <textarea id="notes" placeholder="Write freely here..." style="min-height: 200px; font-size: 15px; line-height: 1.7;"></textarea>
    </div>
  </div>

  <!-- SUBMIT -->
  <div class="submit-section">
    <button class="btn-submit" onclick="submitForm()">Submit & Save Form</button>
    <div class="submit-note">By submitting, you confirm all information above is accurate.</div>
  </div>

</div><!-- end main-form -->

<!-- SUCCESS SCREEN -->
<div class="page-wrap" id="success-screen" style="display:none;">
  <div class="header">
    <div class="spa-name">Madeleine <span>Spa</span></div>
    <div class="spa-tagline">Cliffside Park, New Jersey &nbsp;·&nbsp; Est. 2008</div>
  </div>
  <div class="success-screen" style="display:block;">
    <div class="success-icon">✦</div>
    <div class="success-title">Thank You</div>
    <div class="success-msg" id="successMsg">Your intake form has been received and is on file. We look forward to providing you with an exceptional experience.</div>
    <div style="margin-top:32px;">
      <button class="btn-print" onclick="window.print()">Print / Save PDF</button>
      <button class="btn-new" onclick="resetForm()">New Client Form</button>
    </div>
  </div>
</div>

<script>
  // --- SIGNATURE PAD ---
  const canvas = document.getElementById('sigCanvas');
  const ctx = canvas.getContext('2d');
  let drawing = false;
  let hasSig = false;
  let lastX = 0, lastY = 0;

  function resizeCanvas() {
    const ratio = window.devicePixelRatio || 1;
    const rect = canvas.parentElement.getBoundingClientRect();
    const w = rect.width;
    const h = canvas.offsetHeight || 200;
    canvas.width = w * ratio;
    canvas.height = h * ratio;
    canvas.style.width = w + 'px';
    canvas.style.height = h + 'px';
    ctx.scale(ratio, ratio);
    ctx.strokeStyle = '#3C2A1A';
    ctx.lineWidth = 2;
    ctx.lineCap = 'round';
    ctx.lineJoin = 'round';
  }

  resizeCanvas();
  window.addEventListener('resize', resizeCanvas);

  function getPos(e) {
    const rect = canvas.getBoundingClientRect();
    if (e.touches) {
      return { x: e.touches[0].clientX - rect.left, y: e.touches[0].clientY - rect.top };
    }
    return { x: e.clientX - rect.left, y: e.clientY - rect.top };
  }

  function startDraw(e) {
    e.preventDefault();
    drawing = true;
    hasSig = true;
    document.getElementById('sigPlaceholder').style.opacity = '0';
    document.getElementById('sig-error').style.display = 'none';
    const p = getPos(e);
    lastX = p.x; lastY = p.y;
    ctx.beginPath();
    ctx.moveTo(lastX, lastY);
  }

  function draw(e) {
    if (!drawing) return;
    e.preventDefault();
    const p = getPos(e);
    ctx.lineTo(p.x, p.y);
    ctx.stroke();
    lastX = p.x; lastY = p.y;
  }

  function stopDraw(e) {
    drawing = false;
  }

  canvas.addEventListener('mousedown', startDraw);
  canvas.addEventListener('mousemove', draw);
  canvas.addEventListener('mouseup', stopDraw);
  canvas.addEventListener('mouseleave', stopDraw);
  canvas.addEventListener('touchstart', startDraw, { passive: false });
  canvas.addEventListener('touchmove', draw, { passive: false });
  canvas.addEventListener('touchend', stopDraw);

  function clearSignature() {
    const ratio = window.devicePixelRatio || 1;
    ctx.clearRect(0, 0, canvas.width / ratio, canvas.height / ratio);
    hasSig = false;
    document.getElementById('sigPlaceholder').style.opacity = '1';
  }

  // --- CHECKBOX TOGGLE ---
  function toggleCheck(row) {
    const cb = row.querySelector('input[type="checkbox"]');
    cb.checked = !cb.checked;
  }

  // --- PASTE YOUR GOOGLE APPS SCRIPT WEB APP URL HERE AFTER SETUP ---
  const SCRIPT_URL = 'https://script.google.com/macros/s/AKfycby-eQ07ddaZl3splOtOfWgaLASoXiJDCL10MtF2Lm_xFeOqnvMC_lQfsQZ6Cs7MQcCY/exec';

  // --- VALIDATION & SUBMIT ---
  function required(id, wrapId) {
    const val = document.getElementById(id).value.trim();
    const wrap = document.getElementById(wrapId);
    if (!val) {
      wrap.classList.add('field-error');
      wrap.querySelector('.error-msg').style.display = 'block';
      return false;
    }
    wrap.classList.remove('field-error');
    wrap.querySelector('.error-msg').style.display = 'none';
    return true;
  }

  function submitForm() {
    let valid = true;

    valid = required('firstName', 'f-first') && valid;
    valid = required('lastName', 'f-last') && valid;
    valid = required('address', 'f-address') && valid;
    valid = required('city', 'f-city') && valid;
    valid = required('phone', 'f-phone') && valid;
    valid = required('sigDate', 'f-sigdate') && valid;

    if (!hasSig) {
      document.getElementById('sig-error').style.display = 'block';
      valid = false;
    }

    if (!document.getElementById('agreeCheck').checked) {
      alert('Please read and agree to the Liability Waiver before submitting.');
      valid = false;
    }

    if (!valid) {
      document.querySelector('.field-error')?.scrollIntoView({ behavior: 'smooth', block: 'center' });
      return;
    }

    const btn = document.querySelector('.btn-submit');
    btn.textContent = 'Saving...';
    btn.disabled = true;

    const firstName = document.getElementById('firstName').value.trim();
    const lastName  = document.getElementById('lastName').value.trim();
    const name = firstName + ' ' + lastName;

    const medLabels = {
      lupus:'Lupus or auto-immune deficiency', pregnant:'Currently pregnant',
      bleeding:'Bleeding abnormalities', accutane:'Accutane in last year',
      keloid:'Keloid/thick scarring', embolism:'Pulmonary embolism/blood clot',
      bloodthin:'Blood thinning medication', acne:'Cystic acne',
      diabetes:'Diabetes', darkspots:'Dark spots after pregnancy/injury',
      hepatitis:'Hepatitis/liver condition', hirsutism:'Hirsutism (HR)',
      chempeels:'Chemical peels/dermabrasion', laserlift:'Laser resurfacing/face lift',
      ovarian:'Polycystic ovarian disease', period:'Currently on period',
      varicose:'Varicose vein', circulatory:'Circulatory problems',
      pacemaker:'Pacemaker', rheumatoid:'Rheumatoid arthritis gold therapy',
      epilepsy:'Light sensitive epilepsy', tetracycline:'Tetracycline treatment',
      psoriasis:'Psoriasis or vitiligo', legulcer:'Leg ulcer/phlebitis',
      coumadin:'Coumadin/anti-clotting agents', herpes:'Herpes simplex/fever blisters',
      scars:'Scars that turn white or brown', hiv:'HIV',
      waxing:'Waxing/plinking/electrolysis', transplant:'Transplant anti-rejection drugs',
      tattoo:'Tattoo/permanent make-up', implants:'Any implants', highpressure:'High blood pressure'
    };
    const checked = [...document.querySelectorAll('input[name="med"]:checked')]
      .map(c => medLabels[c.value] || c.value).join('; ') || 'None';

    const payload = {
      timestamp:      new Date().toLocaleString('en-US', {timeZone:'America/New_York'}),
      firstName, lastName,
      address:        document.getElementById('address').value.trim(),
      city:           document.getElementById('city').value.trim(),
      state:          document.getElementById('state').value.trim(),
      zip:            document.getElementById('zip').value.trim(),
      phone:          document.getElementById('phone').value.trim(),
      email:          document.getElementById('email').value.trim(),
      medicalHistory: checked,
      fillerLocation: document.getElementById('fillerLocation').value.trim(),
      fillerDate:     document.getElementById('fillerDate').value,
      medications:    document.getElementById('medications').value.trim(),
      notes:          document.getElementById('notes').value.trim(),
      signatureImage: canvas.toDataURL('image/png'),
      signatureDate:  document.getElementById('sigDate').value,
      agreedToWaiver: 'Yes'
    };

    if (!SCRIPT_URL || SCRIPT_URL === 'PASTE_YOUR_SCRIPT_URL_HERE') {
      showSuccess(name);
      btn.textContent = 'Submit & Save Form';
      btn.disabled = false;
      return;
    }

    fetch(SCRIPT_URL, {
      method: 'POST',
      mode: 'no-cors',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload)
    })
    .then(() => showSuccess(name))
    .catch(() => showSuccess(name))
    .finally(() => {
      btn.textContent = 'Submit & Save Form';
      btn.disabled = false;
    });
  }

  function showSuccess(name) {
    document.getElementById('main-form').style.display = 'none';
    document.getElementById('success-screen').style.display = 'block';
    document.getElementById('successMsg').textContent =
      `Thank you, ${name.split(' ')[0]}. Your intake form has been received and saved to file. We look forward to providing you with an exceptional experience at Madeleine Spa NJ.`;
    window.scrollTo(0, 0);
  }

  function resetForm() {
    document.getElementById('main-form').style.display = 'block';
    document.getElementById('success-screen').style.display = 'none';
    document.getElementById('main-form').querySelectorAll('input[type="text"], input[type="email"], input[type="tel"], input[type="date"], textarea').forEach(el => el.value = '');
    document.querySelectorAll('input[type="checkbox"]').forEach(cb => cb.checked = false);
    clearSignature();
    window.scrollTo(0, 0);
  }

  // Set today's date as default
  const today = new Date().toISOString().split('T')[0];
  document.getElementById('sigDate').value = today;
</script>

</body>
</html>
